// Theme interface to expand themes with more properties in the future
export interface Theme {
    name: string;
    backgroundColor: string;
    cardBack: string;
    buttonColor: number;
    textColor: string;
}
